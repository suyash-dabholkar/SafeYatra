#pragma once
// Copy to secrets.h and fill in. secrets.h is gitignored — NEVER commit it.
#define WIFI_SSID     "your-wifi"
#define WIFI_PASSWORD "your-password"
#define BAND_UUID     "CROWD-0001"   // set per unit at provisioning
