# Copy to backend/.env and fill in. NEVER commit .env.
FLASK_ENV=development
PORT=5000
DATABASE_URL=sqlite:///safeyatra.db
MQTT_BROKER=localhost
MQTT_PORT=1883
# MAP_API_KEY=        # only if using Google Maps; prefer Leaflet/OSM (no key)
